import Next from "src/components/atoms/logos/next";

export interface LogosProps {
  width?: number | string;
  height?: number | string;
  className?: string;
}

const Logos = {
  Next: Next,
};

export default Logos;
